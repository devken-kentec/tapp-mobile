export interface Tarefa {
  id: number;
  inicio: string;
  entrega: string;
  hora: string;
  prioridade: string;
  descricao: string;
  observacao: string;
  statusTarefa: string;
  usuarioId: number;
}
